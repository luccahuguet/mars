# Mars Change Scorecard

Every Mars-owned code or runtime-behavior commit gets a row here. Pure documentation-only commits do not need scorecard rows. The goal is to make the fork delta easy to audit before pulling upstream Rio.

## Rules

- Add or update a row before closing the Bead for a code or runtime-behavior commit
- Keep the reason concrete: what broke, what user workflow it enables, or what gate it supports
- Record touched upstream Rio-owned files explicitly
- Record verification, even when verification is only static review
- For Rio-owned source edits, name the Ghostty and WezTerm equivalent paths or docs, the Rio GitHub issue search, and the newer upstream Rio commit range checked before editing
- Mark upstream merge cost as `low`, `medium`, or `high`
- Confirm the change is the smallest, simplest, surgical, non-invasive path; if not, explain the exception in that cell

## Scorecard

| Commit | Bead | Why It Was Needed | Main Change | Rio-Owned Files | Verification | Merge Cost | Surgical? |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `pending` | `yzt-remove-serenityos-emoji-profile-tce` | Default Yazelix Next installs pulled the expensive `serenityos-emoji-font` build through Mars even though the default Mars/Yazelix profile uses Noto emoji. | Remove the packaged SerenityOS emoji profile from the default Mars package, package metadata, generated config roots, and README support surface. | None. | Verified `nix-instantiate --parse pkgMars.nix`; `nix eval --json .#packages.x86_64-linux.mars.passthru.marsPackageMetadata --no-write-lock-file` lists only `noto` and `twitter` in `supported_emoji_fonts`; `nix build .#mars --no-link --print-build-logs` -> `/nix/store/f3rrx5szwvn7gw92p21l1z3765y40rhm-mars`; generated config tree includes only base and `emoji/twitter` config roots; `! nix path-info -r .#mars \| rg -i 'serenityos\|nanoemoji'`; `git diff --check`. | low | yes |
| `31a66d2fb8` | `yzt-yio` | `force-theme` only selected window/system appearance and did not give Mars users a reliable semantic light terminal palette. | Add `[mars.appearance] preset = "dark" \| "light" \| "auto"` resolution, let `MARS_APPEARANCE` override that config for package consumers, resolve dark/light into the existing `Config.colors` plus `force_theme`, and switch generated/private Mars defaults to the native preset. | `rio-backend/src/config/mod.rs`, `rio-backend/src/config/mars.rs` | Reference gate: Ghostty `src/termio/Termio.zig`, `src/config/conditional.zig`, and `src/config/theme.zig` keep palette/background/foreground and dark/light state as terminal config concepts; WezTerm `config/src/config.rs`, `config/src/color.rs`, and `wezterm-gui` palette consumers keep colors as config-level palette data. `git fetch rio-upstream main`; `git rev-list --count HEAD..rio-upstream/main` returned `10`; touched-path log over Mars config/application paths found only `89c5744eff attempt drop issue on linux (#1666)`, not a replacement for this Mars preset hook. Verified `cargo fmt -- --check --color always`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland appearance`; `cargo clippy -p rio-backend --all-targets --features rio-window/x11,rio-window/wayland` with existing warnings; `nix eval --json .#packages.x86_64-linux.mars.passthru.marsPackageMetadata --no-write-lock-file`; `nix build .#mars --no-link --print-out-paths --print-build-logs` -> `/nix/store/50r97a6zl6w3fl6ril274s6d5n6ybihy-mars`; built config grep confirmed `[mars.appearance]` in every package config root and no stale `[yazelix.appearance]` or `force-theme`; `git diff --check`. | low | yes |
| `63d26976d3` | `yzt-8tg` | Mars low-level split cursor fields were too manual for Yazelix users and duplicated the semantic cursor registry already exposed by Yazelix Cursors. | Add `[yazelix.cursor] preset = "name"` resolution for the current Yazelix cursor registry, keep manual split cursor parsing strict, expose preset metadata in the Mars package, and switch generated/private Mars defaults to `preset = "reef"`. | `rio-backend/src/config/mod.rs`, `rio-backend/src/config/yazelix.rs` | Registry gate: checked main Yazelix `yazelix_cursors_default.toml` and sibling `yazelix-cursors/yazelix_cursors_default.toml`; both expose `blaze`, `snow`, `ice`, `midnight`, `cosmic`, `ocean`, `forest`, `sunset`, `eclipse`, `dusk`, `orchid`, `reef`, and `magma`. Reference gate: Ghostty `src/config/Config.zig` and `src/termio/Termio.zig` expose low-level `cursor-color`, `cursor-style`, `config-file`, and `custom-shader`; WezTerm `config/src/config.rs`, `config/src/color.rs`, and `config/src/scheme_data.rs` expose color schemes and low-level cursor colors. Rio issue searches for `yazelix cursor preset config` found no matches; `cursor color config` returned unrelated color/font issues. `git fetch rio-upstream main`; `git rev-list --count HEAD..rio-upstream/main` returned `10`; touched-path log over the Yazelix config/parser/package paths returned no commits. Verified `cargo fmt -- --check --color always`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland yazelix`; `cargo clippy -p rio-backend --all-targets --features rio-window/x11,rio-window/wayland` with existing warnings; `nix eval --json .#packages.x86_64-linux.mars.passthru.marsPackageMetadata --no-write-lock-file`; `nix build .#mars --no-link --print-build-logs` -> `/nix/store/5lni215z68m6a3z2bi5sz78ngi7rq1d2-mars`; built config grep confirmed `preset = "reef"` in every package config root; `git diff --check`. | low | yes |
| `260a862d02` | `yzt-mars-quit-modal-buttons-08t` | The polished quit confirmation modal still had a decorative top notch and mismatched button colors, making it less calm than the Ghostty reference dialog. | Remove the top accent/notch, widen the standard action buttons, and use one shared grey button treatment for both actions. | `frontends/rioterm/src/router/routes/dialog.rs` | Reference gate: Ghostty `src/Surface.zig` confirm-close decision path and `src/input/command.zig` close-window action checked; WezTerm `wezterm-gui/src/overlay/confirm_close_pane.rs` and `wezterm-gui/src/termwindow/mod.rs` checked for close-confirm overlay flow. Rio issue search for `confirm quit modal close dialog` found no matches; `git fetch rio-upstream main` found two upstream commits ahead with no touched dialog/application/router paths. Verified before cleanup: `cargo fmt -- --check --color always`; `cargo test -p rioterm dialog::tests:: --features wgpu`; `cargo test -p rioterm --features wgpu`; `git diff --check`; `nix build .#mars --no-link --print-build-logs`; `hm-switch-cool 80 --override-input yazelix/mars path:/home/lucca/pjs/yazelix-dir/mars`. | low | yes |
| `ca72d7c581` | `yzt-t6w7.8.1` | Mars status glyphs still sat high compared with Ghostty after sizing fixes, and large global line-height changes made the whole terminal density worse. | Pass computed cell metrics into grid glyph emission, use `cell_baseline + bitmap_top` for glyph bearings, constrain glyphs against face metrics, include a metric signature in atlas keys, and set the accepted Mars defaults to JetBrains Mono 16.0 with `line-height = 1.12`. | `frontends/rioterm/src/grid_emit.rs`, `frontends/rioterm/src/screen/mod.rs`, `sugarloaf/src/grid/atlas.rs`, `sugarloaf/src/text.rs` | Reference gate: Ghostty `src/font/Metrics.zig` cell baseline and face offset model, `src/font/face/freetype.zig` baseline-adjusted constraint and final `bitmap_top + cell_baseline` placement; WezTerm `wezterm-gui/src/utilsprites.rs`, `glyphcache.rs`, and `screen_line.rs` line-height/descender adjustment checked. Rio issue searches for `glyph baseline font metrics line height` and `Nerd Font emoji vertical positioning` found no matches; `git fetch rio-upstream main` found only non-overlapping upstream changes. Verified before cleanup: `cargo fmt -- --check --color always`; `cargo check -p rioterm --features wgpu`; `cargo test -p rioterm grid_emit::cursor_tests:: --features wgpu`; `nix eval --json .#packages.x86_64-linux.mars.passthru.marsPackageMetadata --no-write-lock-file`; `nix build .#mars --no-link --print-out-paths --print-build-logs` -> `/nix/store/jb7cf7kbyb0pbi6izlzda5mb5g6y1pga-mars`; `hm-switch-cool 80 --override-input yazelix/mars path:/home/lucca/pjs/yazelix-dir/mars`; maintainer visually confirmed the runtime. | medium | yes |
| `f8e5fa60ee` | `yzt-t6w7.8`, follow-up `yzt-t6w7.8.2` | Mars symbol/status glyphs could still render too small when a broad symbol needed Ghostty-style access to an adjacent blank cell. | Mirror Ghostty symbol `constraint_width` neighbor rules and split constrained atlas keys by width so one-cell and two-cell versions do not reuse each other. | `frontends/rioterm/src/grid_emit.rs` | Reference gate: Ghostty `src/renderer/cell.zig`, `src/renderer/generic.zig`, and `src/font/Glyph.zig`; WezTerm `wezterm-gui/src/glyphcache.rs` and `shapecache.rs` checked for comparable square-glyph overflow behavior. Verified before cleanup: `cargo fmt -- --check --color always`; `cargo test -p rioterm grid_emit::cursor_tests:: --features wgpu`; `git diff --check`; runtime visual checks. Kept as a separate commit because `yzt-t6w7.8.2` will test whether the baseline fix made any of this layer redundant. | low | yes |
| `b089bd1d97` | `yzt-t6w7.8`, follow-up `yzt-t6w7.8.3` | Mars status symbols and emoji were visibly mis-sized compared with Ghostty before the baseline fix. | Apply the existing Ghostty-derived Nerd Font constraint table and emoji constraints during grid glyph atlas insertion, including focused status-icon sizing regression tests. | `frontends/rioterm/src/grid_emit.rs` | Reference gate: Ghostty `src/font/nerd_font_attributes.zig`, `src/font/Glyph.zig`, `src/font/SharedGrid.zig`, `src/font/face/freetype.zig`, and `src/font/Metrics.zig`; WezTerm `wezterm-gui/src/glyphcache.rs`, `shapecache.rs`, and `utilsprites.rs`; Rio issue search for `Nerd Font glyph vertical emoji symbol metrics` found no matching issue; upstream touched-path check found no replacement. Verified before cleanup: `cargo fmt -- --check --color always`; `cargo test -p rioterm constrain_rasterized_glyph_preserves_status_icon_weight --features wgpu`; `cargo test -p rioterm cursor_tests:: --features wgpu`; `cargo clippy -p rioterm --all-targets --features wgpu` with existing unrelated warnings; `cargo test -p rioterm --features wgpu`; `git diff --check`; package builds and runtime visual checks. Kept separate because `yzt-t6w7.8.3` will test whether this layer can be simplified after the baseline fix. | low | yes |
| `5820a9abf0` | `yzt-mars-quit-confirm-busy-loop-5ql` | The quit confirmation overlay could self-schedule redraws and peg CPU while the Yazelix startup screen was waiting for input. | Make quit-confirm overlay redraws one-shot on entry, cancel, resize, and scale changes instead of frame-tail self-rescheduling. | `frontends/rioterm/src/application.rs`, `frontends/rioterm/src/router/mod.rs` | Reference gate: Ghostty GTK close-confirmation dialog path and WezTerm confirmation overlay flow checked; Rio issue search for `confirm quit redraw busy loop close confirmation` found no replacement; upstream check found no overlapping application/router fix. Verified before cleanup: `cargo fmt -- --check --color always`; `cargo test -p rioterm redraw_tests:: --features wgpu`; `cargo test -p rioterm --features wgpu`; `cargo clippy -p rioterm --all-targets --features wgpu` with existing unrelated warnings; `git diff --check`; `nix build .#packages.x86_64-linux.mars --no-link --print-out-paths --print-build-logs`. | low | yes |
| `d44cf8e15e` | `yzt-t6w7.9` | The inherited quit prompt was tiny, square, and visually out of place beside Ghostty's close confirmation dialog. | Redesign the Mars quit confirmation modal with larger rounded layout, clearer button spacing, and measured proportions. | `frontends/rioterm/src/router/routes/dialog.rs` | Compared visually against Ghostty close confirmation screenshots; iterated with runtime screenshots for modal size, aspect, and button gap. Verified before cleanup with `cargo fmt -- --check --color always`; focused runtime checks through `hm-switch-cool 80 --override-input yazelix/mars path:/home/lucca/pjs/yazelix-dir/mars`; `git diff --check`. | low | yes |
| `809d8bece0` | `yzt-t6w7` | Mars adaptive theme files overrode palette colors with darkened adaptive values, washing out glider/status colors and weakening contrast versus Ghostty. | Make packaged and private Mars defaults use the intended full palette values consistently across adaptive theme files and `[colors]`. | None. | Compared Mars screenshots against Ghostty; verified generated config and runtime palette behavior before cleanup with `nix-instantiate --parse pkgMars.nix`; `nix eval --json .#packages.x86_64-linux.mars.passthru.marsPackageMetadata --no-write-lock-file`; `nix build .#mars --no-link --print-out-paths --print-build-logs`; `hm-switch-cool 80 --override-input yazelix/mars path:/home/lucca/pjs/yazelix-dir/mars`; `git diff --check`. | low | yes |
| `011d648d83` | `yzt-wgpu-image-source-rect-pgi` | WGPU image slicing interpreted normalized `source_rect.zw` as width/height instead of endpoint coordinates, so nonzero-origin slices could sample the wrong texture region. | Interpolate WGPU image UVs from `source_rect.xy` to `source_rect.zw`, align source-rect comments with the documented ABI, and add endpoint plus Naga WGSL parse/validation regression tests. | `Cargo.lock`, `sugarloaf/Cargo.toml`, `sugarloaf/src/renderer/image.wgsl`, `sugarloaf/src/renderer/image.metal`, `sugarloaf/src/renderer/mod.rs` | Reference gate: Ghostty `src/renderer/image.zig`, `src/renderer/shaders/glsl/image.v.glsl`, and `src/renderer/shaders/shaders.metal` checked; Ghostty's comparable image path passes pixel origin+size, so it is not the same normalized endpoint ABI. WezTerm `window/src/bitmaps/mod.rs`, `window/src/bitmaps/atlas.rs`, `wezterm-gui/src/quad.rs`, and `wezterm-gui/src/termwindow/render/mod.rs` checked; WezTerm stores texture rects as origin+size but expands them through `min_x/max_x/min_y/max_y` before quad UV assignment. Rio issue searches for `wgpu image source_rect`, `image source rect`, and `kitty image wgpu` returned no matches; `git fetch rio-upstream main` and `git rev-list --count HEAD..rio-upstream/main` returned `1`, only `46df835698 flake.lock: Update (#1672)`, with no touched Sugarloaf source/dependency commits. Verified `cargo fmt --all -- --check`; `cargo test -p sugarloaf image_source_rect --features wgpu,rio-window/x11,rio-window/wayland`; `cargo test -p sugarloaf image_source_rect --features rio-window/x11,rio-window/wayland`; `cargo check -p rioterm --features wgpu`; `git diff --check`. Full WGPU runtime validation was not run in this pass. | low | yes |
| `f4894de6ee` | `yzt-clean-rio-rebuild-0kz` | Restart Mars from a clean Rio baseline after the old fork became unusable. | Added the initial clean Mars wrapper identity on top of upstream Rio. | None expected for runtime behavior. | `git log` shows one Mars commit on top of `rio-upstream/main`; dogfooding gate still required. | low | yes |
| `a1165abd23` | `yzt-clean-rio-rebuild-0kz.11` | Mars and Rio need separate config roots without Home Manager pretending to be Rio. | Make Mars default to `~/.config/mars`, support `MARS_CONFIG_HOME`, and keep Yazelix startup in TOML config. | `rio-backend/src/config/mod.rs` | `git diff --check`; `cargo fmt --check`; `python3 -m py_compile tools/mars_private_yazelix.py`. | low | yes |
| `d9e303e8b7` | `yzt-emel` | Raw Mars launches can inherit terminal-session and Vulkan environment, and TOML-owned `yzx enter` exits cleanly without opening a durable dogfood window. | Keep Mars config terminal-only, route Mars through a small Vulkan-safe wrapper, and add a separate `mars-yazelix` launch-command wrapper. | None. | `git diff --check`; `python3 -m py_compile tools/mars_private_yazelix.py`; Home Manager switch; `mars --version`; `timeout 10 mars -e true`; `timeout 10 mars-yazelix -e true`; manual desktop launch. | low | yes |
| `d9e303e8b7` | `yzt-clean-rio-rebuild-0kz.12` | Desktop launchers need stable Mars identity and visible icons during dogfooding. | Install Mars icons at standard hicolor sizes and point the desktop file at Mars metadata. | None. | Home Manager switch and visual launcher check. | low | yes |
| `d9e303e8b7` | `yzt-clean-rio-rebuild-0kz.5` | Mars work needs repeatable resource measurements before feature changes. | Add reproducible perf gate orchestration with saved logs and delegated `pidstat`/`perf` sampling. | None. | `python3 -m py_compile tools/mars_perf_gate.py tools/mars_private_yazelix.py`; `tools/mars_perf_gate.py --help`; process-detection cases reject editor paths and accept Mars/Rio app-id commands; no-running-Mars live sampler returns the expected error; process-group cleanup probes, including a TERM-ignoring child left alive by the earlier implementation; tiny idle suite with `MARS_PERF_ARTIFACT_DIR=/tmp/mars_perf_gate_fresh3 tools/mars_perf_gate.py --suite --scenario idle --seconds 2 --cooldown-seconds 0 --startup-delay 0.5 --pty-lines 0 --scroll-lines 0`; inspected microsecond thread samples and no leftover `mars`/`rio`/`rioterm` processes; full suite still required after runtime is stable. | low | yes |
| `78d340e747` | `yzt-clean-rio-rebuild-0kz.6` | Mars perf comparisons need fixed input bytes so generator cost and changing workloads do not pollute measurements. | Add deterministic corpus generation plus `corpus_replay` suite mode with corpus SHA/metadata recorded in artifacts. | None. | `python3 -m py_compile tools/mars_perf_gate.py`; `git diff --check`; generated a deterministic `scroll_render` corpus and validated metadata; smoke replayed the corpus through `MARS_PERF_ARTIFACT_DIR=/tmp/... tools/mars_perf_gate.py --suite --scenario corpus_replay --corpus /tmp/.../scroll.bin --seconds 2 --cooldown-seconds 0 --startup-delay 0.5 --mars-binary mars`, with `corpus_replay: ok`, `workload_phase=output_done`, and manifest SHA/seed/rows/columns present. | low | yes |
| `1ecfc669f4` | `yzt-clean-rio-rebuild-0kz.6` | The perf gate marked completed finite workloads as failures when Mars exited cleanly after the workload finished. | Treat `exit_status=0` with completed `done`, `output_done`, or `screen_done` phase as an OK suite result. | None. | `python3 -m py_compile tools/mars_perf_gate.py`; tiny `corpus_replay` smoke with generated corpus returned `corpus_replay: ok` and `workload_phase=output_done`; `git diff --check`. | low | yes |
| `f2d1ff45a8` | `yzt-t6w7.3` | Yazi KGP virtual previews omit `c=`/`r=`, which made Mars/Rio render the image as a single cell. | Derive omitted virtual-placement columns/rows from image size and cell metrics. | `rio-backend/src/ansi/kitty_virtual.rs` | Reference gate: Ghostty `src/terminal/kitty/graphics_unicode.zig` and `graphics_storage.zig`; WezTerm `term/src/terminalstate/kitty.rs`, `image.rs`, and `wezterm-gui/src/termwindow/render/mod.rs` with no direct U+10EEEE path found. `git diff --check`; `rustfmt --check rio-backend/src/ansi/kitty_virtual.rs`; Cargo/Nix not run under Mars compile-heavy gate. | low | yes |
| `d9e303e8b7` | `yzt-t6w7.1` | Mars dogfooding needs the same icon/font fallback behavior in the private local launch templates as the installed runtime. | Add the runtime Nerd Font, Noto Symbols2, and Noto Color Emoji symbol-map policy to the private Rio/Mars configs. | None. | `diff -u misc/private_rio/config.toml misc/private_yazelix/config.toml`; TOML parse for private and generated configs; `fc-match` for configured families; `MARS_CONFIG_HOME=$PWD/misc/private_yazelix timeout 20s mars -e true`; `RIO_CONFIG_HOME=$PWD/misc/private_rio timeout 20s rio -e true`; `git diff --check`; `cargo fmt --check`; maintainer manually confirmed Mars icons working on 2026-06-21. | low | yes |
| `c2a49e7421` | `yzt-t6w7.2` | Yazelix runs inside Zellij mouse mode, where Rio's default Alt-click hints lose to app mouse reporting and are awkward for daily links. | Make non-macOS link hints use Ctrl-click, recompute hint hover on modifier changes, fully consume the terminal-owned press, and open the link on release. | `rio-backend/src/config/hints.rs`, `frontends/rioterm/src/application.rs`, `frontends/rioterm/src/mouse/mod.rs`, `frontends/rioterm/src/screen/mod.rs` | Reference gate: Ghostty `src/Surface.zig` ctrl-or-super hyperlink handling and `src/input.zig`; WezTerm `docs/config/mouse.md` Ctrl-click example and `wezterm-gui/src/termwindow/mod.rs`. Rio issue query found open #1278, #1457, #1298, #1610, #1619; `git log HEAD..rio-upstream/main` showed no newer link/hint commits. Verified `cargo fmt --check`; `git diff --check`; `cargo check -p rioterm`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland test_hints_default`; maintainer manually confirmed links working in Mars on 2026-06-21. | low | yes |
| `1a9dc553ec` | `yzt-t6w7.2` | Link hover/click regressed on URLs like `http://localhost:4321/`, with the underline extending outside the activatable URL and opening failing. | Port the archived branch's post-processed hit-span clipping, add localhost/OSC8 regression tests, and launch default URL opens directly instead of through the PTY helper. | `frontends/rioterm/src/hints.rs`, `frontends/rioterm/src/screen/mod.rs`, `rio-backend/src/config/hints.rs` | Archived Mars fixes `ff05f45b9d` and `77e4ffb446` checked; Ghostty `src/terminal/render.zig` hyperlink hover spans and WezTerm `lua-api-crates/spawn-funcs/src/lib.rs` opener path checked; Rio issue search found no matching link-hover/open issue; `git fetch rio-upstream main` and `git rev-list --count HEAD..rio-upstream/main` returned `0`. Verified `cargo fmt --check`; `git diff --check`; `cargo test -p rioterm hints::tests::`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland test_default_regex_matches_schemed_urls`; `cargo check -p rioterm`; Home Manager lock updated to current `rebuild/rio-clean-mars` head; `hm-switch-cool 60`; `mars --version`; `timeout 10 mars -e true`; `timeout 10 mars-yazelix -e true`. | low | yes |
| `2daeff3917` | `yzt-t6w7.2` | Fresh-eyes review found direct URL opens could leave unreaped opener children and unnecessarily bypass cwd-aware opening for non-scheme OSC8 targets. | Reap direct opener children in a detached wait thread, null their stdio, preserve `exec` for non-scheme OSC8 targets, and make URI scheme detection case-insensitive. | `frontends/rioterm/src/hints.rs`, `frontends/rioterm/src/screen/mod.rs` | Reference gate: Ghostty `src/os/open.zig` checked for background reaping; WezTerm `lua-api-crates/spawn-funcs/src/lib.rs` checked for delegated opener behavior; `git fetch rio-upstream main` and `git rev-list --count HEAD..rio-upstream/main` returned `0`. Verified `cargo fmt --check`; `git diff --check`; `cargo test -p rioterm hints::tests::`; `cargo check -p rioterm`. | low | yes |
| `8e43f00c1d` | `yzt-t6w7.2` | A URL followed by sentence punctuation could fail to open when the click landed on the visual URL edge near the trailing punctuation. | Keep URL extraction/opening cleaned to the URL, but allow the immediately adjacent trailing punctuation cell to count as a hit target. | `frontends/rioterm/src/screen/mod.rs`, `rio-backend/src/config/hints.rs` | Reference gate: Ghostty `src/Surface.zig` URL hover/open flow and WezTerm `config/src/config.rs` hyperlink rules checked; Rio issue search for trailing punctuation link clicks found no matching issue; `git fetch rio-upstream main` and `git rev-list --count HEAD..rio-upstream/main` returned `0`. Verified `cargo fmt --check`; `git diff --check`; `cargo test -p rioterm test_link_hit_allows_immediate_trailing_punctuation -- --nocapture`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland test_default_regex_strips_trailing_punctuation_on_urls -- --nocapture`; `cargo test -p rioterm screen::tests:: -- --nocapture`; `cargo check -p rioterm`. | low | yes |
| `d9e303e8b7` | `yzt-t6w7.5.1` | Mars needs a non-shader cursor baseline before integrating Yazelix Cursors colors. | Enable Rio's built-in `[effects] trail-cursor = true` in private Mars/Rio templates and matching Home Manager runtime configs; this is a partial baseline, not full v1 completion. | None. | TOML parse; private Mars/Rio config diff; Home Manager switch; generated Mars/Rio/Mars-Yazelix configs parse and diff empty; `mars --version`; `rio --version`; `timeout 10 mars -e true`; `timeout 10 mars-yazelix -e true`; maintainer manually confirmed the built-in cursor tail animating live in Mars on 2026-06-21; remaining v1 work is feeding monocolor input from `yazelix_cursors`. | low | yes |
| `074330d58f` | `yzt-t6w7.5.1` | Mars needs launch-scoped color input from `yazelix_cursors` without adding shader or renderer delta. | Package `mars-yazelix-cursor-config`, which reads `yzc current --format json` once and writes a launch-local Rio-compatible config with `[colors].cursor`; document split/shader cursor variants as v1 limits. | None. | `python3 -m py_compile tools/mars_yazelix_cursor_config.py tools/mars_private_yazelix.py tools/mars_launch_trace.py tools/mars_perf_gate.py`; helper smoke for absent `[colors]`, existing `cursor`, split-family fallback, no-color disable, and real `yzc`; `git diff --check`; `nix-instantiate --parse pkgMars.nix`; `nix-instantiate --parse /home/lucca/.config/home-manager/modules/mars.nix`; `nix eval .#packages.x86_64-linux.mars.drvPath --raw`; `nix build .#packages.x86_64-linux.mars --no-link --print-out-paths`; packaged helper smoke. | low | yes |
| `c892d2f72a` | `yzt-t6w7.5.1` | Cursor config materialization belongs to Yazelix Cursors, not the Mars fork. | Delete the Mars-packaged cursor config helper and document `yzc materialize rio-compatible-config` as the owner of launch-local `[colors].cursor` generation. | None. | `python3 -m py_compile tools/mars_private_yazelix.py tools/mars_launch_trace.py tools/mars_perf_gate.py`; `git diff --check`; `nix-instantiate --parse pkgMars.nix`; `nix-instantiate --parse /home/lucca/.config/home-manager/modules/mars.nix`; `nix build .#packages.x86_64-linux.mars --no-link --print-out-paths`; checked the built Mars output no longer contains `bin/mars-yazelix-cursor-config`. | low | yes |
| `b545e539ec` | `yzt-t6w7.5.2` | Mars needs to render Yazelix split cursor color options natively before main Yazelix emits Mars cursor config. | Add hard-validated `[yazelix.cursor]` split config and render hard-transition vertical/horizontal split cursor sprites for block, hollow, beam, and underline without shaders or mono fallback for malformed or unsupported split config. | `rio-backend/src/config/mod.rs`, `rio-backend/src/config/yazelix.rs`, `frontends/rioterm/src/grid_emit.rs`, `frontends/rioterm/src/renderer/mod.rs`, `frontends/rioterm/src/screen/mod.rs` | Reference gate: Ghostty `src/font/sprite/draw/special.zig` and `src/renderer/generic.zig`; WezTerm `wezterm-gui/src/customglyph.rs`; Rio issue searches for `split cursor`, `cursor color`, and `cursor shape` found cursor-adjacent issues but no native split cursor implementation; `git fetch rio-upstream main` and `git rev-list --count HEAD..rio-upstream/main` returned `1`, only `46df835698 flake.lock: Update (#1672)`. Verified `cargo fmt --all -- --check`; `git diff --check`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland yazelix`; `cargo test -p rioterm --features rio-window/x11,rio-window/wayland cursor_tests`; `cargo check -p rioterm --features rio-window/x11,rio-window/wayland`; `cargo build -p rioterm --features rio-window/x11,rio-window/wayland`; working-tree CPU split runtime smoke with temporary `[renderer] use-cpu = true` and `[yazelix.cursor]` vertical split config passed `idle` and `pty_flood` via `tools/mars_perf_gate.py --suite --seconds 4 --pty-lines 5000 --internal-metrics`, with 26 idle and 208 pty-flood metrics lines and no invalid metrics. Raw working-tree Vulkan smoke was blocked by host `ERROR_INCOMPATIBLE_DRIVER`, so packaged GPU visual validation is still required. | low | yes |
| `1a80115ef9` | `yzt-t6w7.6` | Mars/Yazelix completion needs a terminal-standard BEL cue without old fork assumptions. | Add `[bell].visual`, draw a short full-window visual flash on BEL, keep audio independent, and enable visual-only private dogfood configs. | `rio-backend/src/config/bell.rs`, `frontends/rioterm/src/application.rs`, `frontends/rioterm/src/constants.rs`, `frontends/rioterm/src/scheduler.rs`, `frontends/rioterm/src/screen/mod.rs` | Reference gate: Ghostty `src/apprt/gtk/class/surface.zig`, `window.zig`, `ui/1.2/surface.blp`, and `css/style.css`; WezTerm `config/src/config.rs`, `wezterm-gui/src/termwindow/mod.rs`, `render/mod.rs`, and bell docs. Rio issues #1616, #1617, #1243, and PR #1284 checked; `git fetch rio-upstream main`; upstream `46df835698` still has audio-only bell config and no newer visual fix. Verified `cargo fmt --check`; `cargo test -p rio-backend bell::tests --features rio-window/x11,rio-window/wayland -- --nocapture`; `cargo check -p rioterm`; `git diff --check`; private config TOML parse and diff; `nix build .#packages.x86_64-linux.mars --no-link --print-out-paths`; built Mars launch with `printf "\a"` captured `/tmp/mars-bell-smoke.png`, `/tmp/mars-bell-active.png`, and `/tmp/mars-bell-after.png`; observed visual-only cue, no audible cue. | low | yes |
| `3dd8210e70` | `yzt-8t5o` | Raw package-level Mars launches failed when the Vulkan loader could not discover usable ICD manifests. | Set a package-owned default `VK_ICD_FILENAMES` from the Nix Mesa ICD directory without overriding user-provided values. | None. | Rio issue searches for `ERROR_INCOMPATIBLE_DRIVER`, `vkCreateInstance Vulkan driver`, and `Vulkan ICD Mesa Wayland` found no matching Rio issue; `git fetch rio-upstream main`; no newer upstream commits touched the relevant Vulkan/window/renderer paths; launch matrix showed clean package env failed without `VK_ICD_FILENAMES` and passed with only `VK_ICD_FILENAMES`; `nix-instantiate --parse pkgMars.nix`; `git diff --check`; `nix build .#packages.x86_64-linux.mars --no-link --print-out-paths`; clean-env `$out/bin/mars -e true` passed. | low | yes |
| `01d732997c` | `yzt-8t5o` | Fresh-eyes review found an empty `VK_ICD_FILENAMES` still bypassed the package default and reproduced the Vulkan launch crash. | Treat unset or empty `VK_ICD_FILENAMES` as missing while preserving non-empty user overrides. | None. | `nix-instantiate --parse pkgMars.nix`; `git diff --check`; `nix build .#packages.x86_64-linux.mars --no-link --print-out-paths`; built wrapper inspection; clean-env `$out/bin/mars -e true` passed with `VK_ICD_FILENAMES` unset and with `VK_ICD_FILENAMES=`; deliberate bad non-empty `VK_ICD_FILENAMES=/tmp/does-not-exist.json` still failed, confirming explicit overrides are preserved. | low | yes |
| `d9e303e8b7` | `yzt-t6w7.4` | Mars/COSMIC/Wayland opacity is blocked by Rio's Vulkan swapchain forcing opaque compositor alpha. | Choose a supported non-opaque Vulkan composite alpha mode, matching Rio PR #1667 and the existing WGPU/WezTerm preference order. | `sugarloaf/src/context/vulkan.rs` | Rio issue #1644 and PR #1667 checked; `git fetch rio-upstream main` plus touched-path log showed no newer merged fix and upstream still hard-codes `OPAQUE`; Rio WGPU and WezTerm WGPU alpha mode selection checked; Ghostty opacity paths checked, with no directly comparable Vulkan swapchain composite-alpha path found in the local reference clone; `cargo fmt --check`; `git diff --check`; Home Manager lock updated to Mars `c014d81197`; `hm-switch-cool 60`; `mars --version`; `rio --version`; `timeout 10 mars -e true`; `timeout 10 mars-yazelix -e true`; A/B on 2026-06-21 held config/env constant: patched `/nix/store/dvhrcfli3fvbap48in25s78la8f04m3z-mars` was transparent and reverted-OPAQUE `/nix/store/0rxsvrpxaz84swa81097wj4586avrf84-mars` was opaque; screenshots saved under `/tmp/mars-alpha-ab-artifacts/`. | low | yes |
| `a650371806` | `yzt-clean-rio-rebuild-0kz.13` | A Mars/Yazelix window disappeared without a final launcher footer, leaving unclear frontend-vs-session evidence. | Package `mars-launch-trace` and route the private desktop wrapper through it for child PID, signal, process-tree, survivor, and final-status breadcrumbs. | None. | Ghostty `src/termio/Exec.zig` and WezTerm `mux/src/localpane.rs` child-watch patterns checked; Rio issue search found crash/process reports but no launcher breadcrumb replacement; `git fetch rio-upstream main` showed no newer upstream commits ahead of this branch; `python3 -m py_compile tools/mars_launch_trace.py tools/mars_private_yazelix.py tools/mars_perf_gate.py`; `git diff --check`; `nix eval .#packages.x86_64-linux.mars.drvPath`; `nix build .#packages.x86_64-linux.mars --no-link --print-out-paths`; normal `/bin/true` smoke logged `mars exit status=0`; forced `/bin/sleep` child kill logged `child_signal=SIGTERM` and `mars exit status=143`; Home Manager lock updated to Mars `a650371806`; `hm-switch-cool 60`; installed `mars-yazelix -e true` logged `mars exit status=0`; installed helper forced-kill smoke logged `child_signal=SIGTERM` and `mars exit status=143`. | low | yes |
| `52952ad0c7` | `yzt-clean-rio-rebuild-0kz.10` | Mars needs disabled-by-default internal timing evidence to explain PTY/render CPU during dogfooding. | Add gated JSONL metrics for PTY read batches, parser/state batches, render snapshots, grid row/cell emit, frame present timing, and perf-gate histogram artifacts via `--internal-metrics`. | `rio-backend/src/lib.rs`, `rio-backend/src/perf_metrics.rs`, `rio-backend/src/performer/mod.rs`, `frontends/rioterm/src/screen/mod.rs` | Reference gate: Ghostty `src/benchmark/TerminalParser.zig`, `TerminalStream.zig`, and `ScreenClone.zig`; WezTerm `wezterm-gui/src/stats.rs`, `quad.rs`, `glyphcache.rs`, `termwindow/mod.rs`, and `window/src/bitmaps/atlas.rs`; Rio issues #1354 and #1589 checked with no internal-metrics fix found; `git fetch rio-upstream main`; `git rev-list --count HEAD..rio-upstream/main` returned `1`, only `46df835698 flake.lock: Update (#1672)`. Verified `python3 -m py_compile tools/mars_perf_gate.py`; `cargo fmt --check`; `git diff --check`; `cargo check -p rioterm`; `nix build .#packages.x86_64-linux.mars --no-link --print-out-paths`; default-off package smoke with `MARS_PERF_METRICS_FILE` set created no metrics file; traced `/tmp/mars-perf-metrics-smoke2` `pty_flood` suite with `--internal-metrics` emitted `mars_perf_metrics.jsonl` and `mars_perf_metrics_summary.json` with `frame_render`, `grid_emit`, `parser_state_batch`, `pty_read_batch`, and `render_snapshot` histograms. | low | yes |
| `fa752259af` | `yzt-clean-rio-rebuild-0kz.10` | Fresh-eyes review found disabled metrics still allocated JSON field strings before checking whether tracing was enabled. | Make `write_event` accept a lazy fields closure so JSON formatting happens only after metrics are active. | `rio-backend/src/perf_metrics.rs` | Same reference gate as `52952ad0c7`: Ghostty `src/benchmark/TerminalParser.zig`, `TerminalStream.zig`, and `ScreenClone.zig`; WezTerm `wezterm-gui/src/stats.rs`, `quad.rs`, `glyphcache.rs`, `termwindow/mod.rs`, and `window/src/bitmaps/atlas.rs`; Rio issues #1354 and #1589 checked with no internal-metrics fix found; `git fetch rio-upstream main`; `git rev-list --count HEAD..rio-upstream/main` returned `1`, only `46df835698 flake.lock: Update (#1672)`. Verified `cargo fmt --check`; `cargo check -p rioterm`; `python3 -m py_compile tools/mars_perf_gate.py`; `git diff --check`; `nix build .#packages.x86_64-linux.mars --no-link --print-out-paths`; default-off package smoke with `MARS_PERF_METRICS_FILE` set created no metrics file; traced `/tmp/mars-perf-metrics-fresh-eyes` `pty_flood` suite with `--internal-metrics` emitted valid `frame_render`, `grid_emit`, `parser_state_batch`, `pty_read_batch`, and `render_snapshot` metrics; no leftover Mars/workload process found. | low | yes |
| `3cf912b9bf` | `yzt-clean-rio-rebuild-0kz.7` | Mars needs parser-only throughput evidence before tuning PTY/parser/render behavior from live dogfooding symptoms. | Add a hidden Rio-backend parser benchmark helper, `mars-parser-bench` checked binary, small smoke fixture, and docs for corpus-based release comparisons. | `rio-backend/src/lib.rs`, `rio-backend/src/benchmarks.rs`, `rio-backend/src/bin/mars-parser-bench.rs`, `rio-backend/fixtures/parser_smoke.txt` | Reference gate: Ghostty `src/benchmark/TerminalParser.zig`; WezTerm `mux/src/lib.rs`, `term/src/terminal.rs`, and `termwiz/benches/cell.rs`; Rio issue search for parser/performance/PTY benchmark found no existing parser benchmark to consume; `git fetch rio-upstream main`; `git rev-list --count HEAD..rio-upstream/main` returned `1`, and touched-path log showed parser/backend commits but no benchmark replacement. Verified `cargo fmt --check`; `cargo check -p rio-backend --bin mars-parser-bench --features rio-window/x11,rio-window/wayland`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland parser_benchmark_counts_control_sequences -- --nocapture`; release smoke on `rio-backend/fixtures/parser_smoke.txt`; generated `/tmp/mars_parser_osc.bin` with `tools/mars_perf_gate.py --generate-corpus`; release run on the generated corpus reported `csi_dispatch_callbacks=8000` and `osc_dispatch_callbacks=4000`; `git diff --check`. | low | yes |
| `af726b52e4` | `yzt-clean-rio-rebuild-0kz.7` | Fresh-eyes review found UTF-8 parser benchmark output undercounted `print_bytes` for `print_codepoints` batches. | Count UTF-8 byte width for non-ASCII codepoint batches and add a UTF-8 regression test. | `rio-backend/src/benchmarks.rs` | Same reference gate as `3cf912b9bf`: Ghostty `src/benchmark/TerminalParser.zig`; WezTerm `mux/src/lib.rs`, `term/src/terminal.rs`, and `termwiz/benches/cell.rs`; Rio issue search and upstream check found no benchmark replacement. Verified `cargo fmt --check`; `cargo check -p rio-backend --bin mars-parser-bench --features rio-window/x11,rio-window/wayland`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland parser_benchmark_counts -- --nocapture`; generated `/tmp/mars_parser_utf8.bin` with `tools/mars_perf_gate.py --generate-corpus --corpus-kind utf8`; release run reported `corpus_bytes=174936`, `print_bytes=172936`, and `execute_callbacks=2000`; `git diff --check`. | low | yes |
| `c69d02a716` | `yzt-clean-rio-rebuild-0kz.8` | Mars needs parser-plus-terminal-state evidence when parser-only throughput is fine but wrapping, scrollback, style, cursor, or grid mutation may dominate PTY-reader CPU. | Add `run_terminal_stream_benchmark`, `mars-terminal-stream-bench`, a small smoke fixture, and clean gate docs for release-mode corpus comparisons. | `rio-backend/src/benchmarks.rs`, `rio-backend/src/bin/mars-terminal-stream-bench.rs`, `rio-backend/fixtures/terminal_stream_smoke.txt` | Reference gate: Ghostty `src/benchmark/TerminalStream.zig`; WezTerm `mux/src/lib.rs` and `term/src/terminal.rs`; Rio issue search for terminal stream/state benchmark and PTY/parser CPU found no existing benchmark to consume; `git fetch rio-upstream main`; `git rev-list --count HEAD..rio-upstream/main` returned `1`, and touched-path log over backend performer/crosswords/benchmark paths found no replacement. Verified `cargo fmt --check`; `git diff --check`; `cargo check -p rio-backend --bin mars-terminal-stream-bench --features rio-window/x11,rio-window/wayland`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland terminal_stream_benchmark -- --nocapture`; generated `/tmp/mars_stream_scroll.bin` with `tools/mars_perf_gate.py --generate-corpus`; release smoke on `rio-backend/fixtures/terminal_stream_smoke.txt`; release run on the generated corpus reported `corpus_metadata_present=true`, `final_history_size=759`, and `final_total_lines=771`. | low | yes |
| `628bc4bf0d` | `yzt-clean-rio-rebuild-0kz.8` | Fresh-eyes review found the terminal-stream CLI only reported sidecar metadata path/size, which made archived stdout insufficient to identify corpus seed/SHA after `/tmp` cleanup. | Add a one-line `corpus_metadata_json` output field and move the parser comparison paragraph back under the parser benchmark docs. | `rio-backend/src/bin/mars-terminal-stream-bench.rs` | Same reference gate as `c69d02a716`: Ghostty `src/benchmark/TerminalStream.zig`; WezTerm `mux/src/lib.rs` and `term/src/terminal.rs`; Rio issue search and upstream check found no replacement. Verified `cargo fmt --check`; `git diff --check`; `cargo check -p rio-backend --bin mars-terminal-stream-bench --features rio-window/x11,rio-window/wayland`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland terminal_stream_benchmark -- --nocapture`; release run on `/tmp/mars_stream_scroll.bin` showed `corpus_metadata_json={...}` with seed/SHA; release smoke fixture showed missing metadata as `corpus_metadata_json=`. | low | yes |
| `92d852a070` | `yzt-clean-rio-rebuild-0kz.8` | Fresh-eyes review found `corpus_metadata_present` depended on UTF-8 parsing instead of sidecar existence, contradicting the path/size fields. | Report sidecar presence and byte size from filesystem metadata while keeping `corpus_metadata_json` dependent on UTF-8 reading. | `rio-backend/src/bin/mars-terminal-stream-bench.rs` | Same reference gate as `c69d02a716`: Ghostty `src/benchmark/TerminalStream.zig`; WezTerm `mux/src/lib.rs` and `term/src/terminal.rs`; Rio issue search and upstream check found no replacement. Verified `cargo fmt --check`; `git diff --check`; `cargo check -p rio-backend --bin mars-terminal-stream-bench --features rio-window/x11,rio-window/wayland`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland terminal_stream_benchmark -- --nocapture`; release generated-corpus replay still emitted JSON metadata; release smoke with a non-UTF-8 sidecar reported `corpus_metadata_present=true`, `corpus_metadata_bytes=5`, and empty `corpus_metadata_json`. | low | yes |
| `9f6d3a8fcd` | `yzt-clean-rio-rebuild-0kz.9` | Mars needs renderer-facing snapshot and damage extraction evidence before tuning render CPU from dogfooding symptoms. | Add `run_render_snapshot_benchmark`, checked `mars-render-snapshot-bench`, a smoke fixture, and clean gate docs for release-mode snapshot comparisons. | `rio-backend/src/benchmarks.rs`, `rio-backend/src/bin/mars-render-snapshot-bench.rs`, `rio-backend/fixtures/render_snapshot_smoke.txt` | Reference gate: Ghostty `src/benchmark/ScreenClone.zig` and `src/benchmark/TerminalStream.zig`; WezTerm `mux/src/localpane.rs`, `wezterm-gui/src/termwindow/mod.rs`, and `term/src/screen.rs`; Rio issue searches for render snapshot/damage benchmark and renderer CPU/damage found no existing benchmark to consume; `git fetch rio-upstream main`; touched-path log from merge base `e1946a7b98a5a5a4074f384437f0256abf1df75b` to `rio-upstream/main` over backend benchmark/crosswords and rioterm renderer/screen paths returned no replacement commits. Verified `cargo fmt --check`; `git diff --check`; `cargo check -p rio-backend --bin mars-render-snapshot-bench --features rio-window/x11,rio-window/wayland`; `cargo test -p rio-backend --features rio-window/x11,rio-window/wayland render_snapshot_benchmark -- --nocapture`; release smoke on `rio-backend/fixtures/render_snapshot_smoke.txt` reported `Noop` with zero dirty rows, `Full` with 24 dirty rows, `Partial` with 4 dirty rows, and `final_extras_count=1`; `--mode noop --dirty-rows 0` succeeds while `--mode incremental --dirty-rows 0` fails with the expected validation error; generated `/tmp/mars_snapshot_scroll.bin` with `tools/mars_perf_gate.py --generate-corpus` using `scroll_render` seed 7, 80000 lines, 44 rows, and 132 columns; release generated-corpus run reported sidecar metadata with seed/SHA, `final_history_size=10000`, `Full` with 44 dirty rows, and `Partial` with 8 dirty rows. | low | yes |

## Commit Template

| Commit | Bead | Why It Was Needed | Main Change | Rio-Owned Files | Verification | Merge Cost | Surgical? |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `shortsha` | `bead-id` | One sentence. | One sentence. | `path` or `None`. | Include Ghostty/WezTerm refs, Rio issue query, upstream commit range, and commands/artifacts. | low/medium/high | yes or exception |
